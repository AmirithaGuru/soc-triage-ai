# Dev / test dependencies (never installed in production or on Lambda)
# Install with: python3 -m pip install -r requirements-dev.txt
# This automatically pulls in all production deps first via the line below.

-r requirements.txt

# Testing
pytest==9.0.3
pytest-asyncio==0.25.3
pytest-httpx==0.35.0

# Load testing
locust==2.32.3

# Security scanning
bandit==1.8.3
detect-secrets==1.5.0

# Linting / formatting
ruff==0.15.11
