"""Integration tests for all six API endpoints against a running app with a real (test) database."""
# Placeholder — tests will be added when all endpoints and the database layer are implemented in Phase 1.
